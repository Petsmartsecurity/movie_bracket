{
  "name": "movie-bracket",
  "version": "1.0.0",
  "scripts": {
    "dev:api": "node api/dev-server.js",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client"
  },
  "dependencies": {
    "@vercel/kv": "^3.0.0"
  }
}
